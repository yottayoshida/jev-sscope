{
  "name": "jev-sscope",
  "version": "0.1.0",
  "private": true,
  "description": "Watch a Claude Code session while it runs, scored step by step by Jev on Cloudflare Workers AI",
  "scripts": {
    "dev": "wrangler dev",
    "check": "tsc -p tsconfig.json",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.ts && python3 -m unittest discover -s client -p 'test_*.py'"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260919.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.135.0"
  },
  "type": "module"
}
